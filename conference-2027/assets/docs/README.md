# Documents

Put downloadable PDFs here, then point to them in `data/config.js`:

- Programme → set `programmePdf: "assets/docs/programme.pdf"`

The **Download programme** button on the site appears automatically when the path is set,
and stays hidden while the value is `""`.
